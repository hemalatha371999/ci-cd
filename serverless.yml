service: lambda-service-github
provider:
  name: aws
  runtime: nodejs20.x
  region: ap-south-1
functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: /
          method: get
